{
  "name": "nabd-alriyadah",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fetch:news": "node scripts/fetch-news.mjs",
    "rewrite:articles": "node scripts/rewrite-article.mjs",
    "generate:images": "node scripts/generate-images.mjs",
    "automation:content": "npm run fetch:news && npm run rewrite:articles && npm run generate:images"
  },
  "dependencies": {
    "cloudinary": "^2.5.1"
  }
}
